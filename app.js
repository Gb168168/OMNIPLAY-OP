const $=s=>document.querySelector(s);const LOCAL_KEY='omniplay-workspace-v3';const state={categories:[],activeCategoryId:null,activePageId:null};let currentUniver=null;const uid=(p='id')=>`${p}_${Date.now()}_${Math.random().toString(36).slice(2,8)}`;const esc=(s='')=>String(s).replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));const activeCategory=()=>state.categories.find(c=>c.id===state.activeCategoryId);const activePage=()=>activeCategory()?.pages?.find(p=>p.id===state.activePageId);const typeIcon=t=>({sheet:'📊',files:'📄',photos:'🖼️',videos:'🎬'})[t]||'📄';function load(){try{state.categories=JSON.parse(localStorage.getItem(LOCAL_KEY)||localStorage.getItem('omniplay-workspace-v2')||'[]')}catch{state.categories=[]}$('#cloudStatus').textContent='🧪 測試模式・本機儲存';renderNav();renderPage()}function save(){localStorage.setItem(LOCAL_KEY,JSON.stringify(state.categories));$('#cloudStatus').textContent='✓ 已儲存'}function disposeSheet(){try{currentUniver?.univer?.dispose?.()}catch{}currentUniver=null}function renderNav(){const root=$('#categoryList');root.innerHTML='';state.categories.forEach(cat=>{const box=document.createElement('div');box.className='category';box.innerHTML=`<div class="category-head ${cat.id===state.activeCategoryId?'active':''}"><span>📁 ${esc(cat.name)}</span><button class="mini">⋯</button></div><div class="category-pages"></div>`;box.querySelector('.category-head').onclick=e=>{if(e.target.classList.contains('mini'))return;state.activeCategoryId=cat.id;state.activePageId=cat.pages?.[0]?.id||null;renderNav();renderPage()};box.querySelector('.mini').onclick=()=>{if(confirm(`刪除分類「${cat.name}」？`)){state.categories=state.categories.filter(x=>x.id!==cat.id);state.activeCategoryId=null;state.activePageId=null;save();renderNav();renderPage()}};const pages=box.querySelector('.category-pages');(cat.pages||[]).forEach(p=>{const row=document.createElement('div');row.className=`page-row ${p.id===state.activePageId?'active':''}`;row.innerHTML=`<button class="page-link">${typeIcon(p.type)} ${esc(p.name)}</button><button class="page-delete">×</button>`;row.querySelector('.page-link').onclick=()=>{state.activeCategoryId=cat.id;state.activePageId=p.id;renderNav();renderPage()};row.querySelector('.page-delete').onclick=()=>{if(confirm(`刪除頁面「${p.name}」？`)){cat.pages=cat.pages.filter(x=>x.id!==p.id);state.activePageId=cat.pages[0]?.id||null;save();renderNav();renderPage()}};pages.appendChild(row)});root.appendChild(box)});$('#addPageBtn').disabled=!state.activeCategoryId}function renderPage(){disposeSheet();const page=activePage(),cat=activeCategory();$('#emptyState').classList.toggle('hidden',!!page||!!cat);$('#workspace').classList.toggle('hidden',!page);$('#breadcrumb').textContent=cat?`工作區 / ${cat.name}`:'工作區';$('#pageTitle').textContent=page?page.name:(cat?cat.name:'歡迎使用 OMNIPLAY');if(!page){if(cat)$('#emptyState').innerHTML='<div><h2>這個分類還沒有頁面</h2><p>按右上角「新增頁面」開始。</p></div>';return}page.type==='sheet'?renderUniverSheet(page):renderFiles(page)}function renderUniverSheet(page){const ws=$('#workspace');ws.classList.add('sheet-workspace');ws.innerHTML='<div class="sheet-note">📊 專業試算表測試版・繁體中文・資料庫稍後連接</div><div id="univerSheet" class="univer-sheet"></div>';try{if(!window.UniverPresets||!window.UniverPresetSheetsCore||!window.UniverCore)throw new Error('Univer CDN 尚未載入');const{createUniver}=window.UniverPresets,{LocaleType,mergeLocales}=window.UniverCore,{UniverSheetsCorePreset}=window.UniverPresetSheetsCore;const locale=window.UniverPresetSheetsCoreZhTW;if(!locale)throw new Error('Univer 繁體中文語言包尚未載入');const zhTW=LocaleType.ZH_TW||'zh-TW';const{univer,univerAPI}=createUniver({locale:zhTW,locales:{[zhTW]:mergeLocales(locale)},presets:[UniverSheetsCorePreset({container:'univerSheet'})]});univerAPI.createWorkbook(page.snapshot||{name:page.name});currentUniver={univer,univerAPI};$('#cloudStatus').textContent='📊 試算表・繁體中文'}catch(err){console.error(err);ws.innerHTML=`<div class="notice">試算表元件載入失敗，請 Ctrl+F5 重新整理。<br>${esc(err.message)}</div>`}}function renderFiles(page){$('#workspace').classList.remove('sheet-workspace');page.files||=[];const title=page.type==='photos'?'照片':page.type==='videos'?'影片':'檔案 / PDF',ws=$('#workspace');ws.innerHTML=`<div class="notice">🧪 測試版：目前只測試分類與檔案介面，雲端儲存稍後連接。</div><div class="file-panel"><div id="uploadZone" class="upload-zone"><strong>＋ 加入${title}</strong><div class="file-meta">目前不會上傳到網路</div></div><div id="fileGrid" class="file-grid"></div></div>`;const input=$('#hiddenUpload');input.accept=page.type==='photos'?'image/*':page.type==='videos'?'video/*':'.pdf,.doc,.docx,.xls,.xlsx,.csv,.txt,application/pdf';$('#uploadZone').onclick=()=>input.click();input.onchange=e=>{[...e.target.files].forEach(file=>{if(page.type==='photos'&&!file.type.startsWith('image/'))return;if(page.type==='videos'&&!file.type.startsWith('video/'))return;page.files.push({id:uid('file'),name:file.name,type:file.type,size:file.size})});save();renderFiles(page);e.target.value=''};const grid=$('#fileGrid');if(!page.files.length)grid.innerHTML='<div class="file-meta">目前沒有檔案。</div>';page.files.forEach(f=>{const card=document.createElement('div');card.className='file-card';card.innerHTML=`<div class="file-placeholder">${f.type?.startsWith('image/')?'🖼️':f.type?.startsWith('video/')?'🎬':'📄'}</div><div class="file-name">${esc(f.name)}</div><div class="file-meta">${formatBytes(f.size||0)}</div><button class="remove-file">刪除</button>`;card.querySelector('.remove-file').onclick=()=>{page.files=page.files.filter(x=>x.id!==f.id);save();renderFiles(page)};grid.appendChild(card)})}function formatBytes(n){if(!n)return'0 B';const u=['B','KB','MB','GB'];let i=0;while(n>=1024&&i<u.length-1){n/=1024;i++}return`${n.toFixed(i?1:0)} ${u[i]}`}$('#addCategoryBtn').onclick=()=>{$('#categoryName').value='';$('#categoryDialog').showModal()};$('#categoryForm').onsubmit=e=>{e.preventDefault();const name=$('#categoryName').value.trim();if(!name)return;const cat={id:uid('cat'),name,pages:[]};state.categories.push(cat);state.activeCategoryId=cat.id;state.activePageId=null;save();$('#categoryDialog').close();renderNav();renderPage()};$('#addPageBtn').onclick=()=>{$('#pageName').value='';$('#pageDialog').showModal()};$('#pageForm').onsubmit=e=>{e.preventDefault();const cat=activeCategory(),name=$('#pageName').value.trim(),type=$('#pageType').value;if(!cat||!name)return;const page={id:uid('page'),name,type,createdAt:new Date().toISOString()};if(type!=='sheet')page.files=[];cat.pages.push(page);state.activePageId=page.id;save();$('#pageDialog').close();renderNav();renderPage()};load();